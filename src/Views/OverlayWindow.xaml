<Window x:Class="EasyScreenRecord.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OverlayWindow" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        MouseDown="Window_MouseDown"
        MouseMove="Window_MouseMove"
        MouseUp="Window_MouseUp"
        KeyDown="Window_KeyDown">
    <Grid>
        <!-- Background Dimming (Opacity Mask will be used to cut hole) -->
        <Path x:Name="DimmingLayer" Fill="#A0000000" IsHitTestVisible="False">
            <Path.Data>
                <CombinedGeometry GeometryCombineMode="Exclude">
                    <CombinedGeometry.Geometry1>
                        <RectangleGeometry Rect="0,0,3840,2160" /> <!-- Large enough to cover screen -->
                    </CombinedGeometry.Geometry1>
                    <CombinedGeometry.Geometry2>
                        <RectangleGeometry x:Name="SelectionRectGeometry" Rect="0,0,0,0" />
                    </CombinedGeometry.Geometry2>
                </CombinedGeometry>
            </Path.Data>
        </Path>

        <!-- Selection Border -->
        <Border x:Name="SelectionBorder" BorderBrush="#00FF00" BorderThickness="2" Visibility="Collapsed" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        
        <!-- Info Text -->
        <TextBlock Text="Drag to select area. Press ESC to cancel, ENTER to confirm." 
                   Foreground="White" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0">
            <TextBlock.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="5"/>
            </TextBlock.Effect>
        </TextBlock>
    </Grid>
</Window>
