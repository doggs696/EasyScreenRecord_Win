<Window x:Class="EasyScreenRecord.Views.KeyOSDWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KeyOSDWindow" Height="100" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        IsHitTestVisible="False" Focusable="False">
    
    <Grid>
        <Border Background="#CC000000" CornerRadius="10" Padding="20,10"
                HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="5"/>
            </Border.Effect>
            <TextBlock x:Name="KeyText" Text="Ctrl + C" Foreground="White" 
                       FontSize="32" FontWeight="Bold" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
