<Window x:Class="EasyScreenRecord.Views.RecordingFrameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RecordingFrameWindow" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        MouseDown="Window_MouseDown">
    <Grid>
        <!-- Dimming Layer with cutout for the recording region -->
        <Path x:Name="DimmingLayer" Fill="#60000000" Stroke="{x:Null}">
            <Path.Data>
                <CombinedGeometry GeometryCombineMode="Exclude">
                    <CombinedGeometry.Geometry1>
                        <RectangleGeometry x:Name="FullScreenGeometry" Rect="0,0,3840,2160" />
                    </CombinedGeometry.Geometry1>
                    <CombinedGeometry.Geometry2>
                        <RectangleGeometry x:Name="CutoutGeometry" Rect="0,0,0,0" />
                    </CombinedGeometry.Geometry2>
                </CombinedGeometry>
            </Path.Data>
        </Path><!--/DimmingLayer-->

        <!-- Recording Region Border (inner frame around the cutout) -->
        <Border x:Name="FrameBorder" 
                Panel.ZIndex="10"
                BorderBrush="#7FFF00" 
                BorderThickness="4" 
                CornerRadius="0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                IsHitTestVisible="False"/>
    </Grid>
</Window>
