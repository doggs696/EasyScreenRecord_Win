<Window x:Class="EasyScreenRecord.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:vm="clr-namespace:EasyScreenRecord.ViewModels"
        Title="EasyScreenRecord" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">
    
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <!-- Tray Icon -->
        <tb:TaskbarIcon x:Name="MyNotifyIcon"
                        ToolTipText="EasyScreenRecord"
                        IconSource="/Assets/tray_icon.png"
                        DoubleClickCommand="{Binding ShowMainWindowCommand}">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu Background="#FFFFFF">
                    <MenuItem Header="Start Recording" Command="{Binding ToggleRecordingCommand}" Foreground="#000000" Background="#FFFFFF" />
                    <MenuItem Header="Settings" Command="{Binding ShowSettingsCommand}" Foreground="#000000" Background="#FFFFFF" />
                    <Separator Background="#CCCCCC"/>
                    <MenuItem Header="Exit" Command="{Binding ExitAppCommand}" Foreground="#000000" Background="#FFFFFF" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        <!-- Main Content -->
        <StackPanel Margin="20">
            <TextBlock x:Name="StatusText" Text="Ready" Margin="0,0,0,10" FontSize="16"/>
            
            <!-- Recording Mode Selection -->
            <TextBlock Text="録画モード:" Margin="0,0,0,5" FontWeight="Bold"/>
            <Button x:Name="FullscreenButton" Content="全画面録画" Command="{Binding StartFullscreenRecordingCommand}" Margin="0,0,0,5" Padding="10,5" Background="#4CAF50" Foreground="White"/>
            <Button x:Name="RegionButton" Content="範囲選択録画" Command="{Binding StartRegionRecordingCommand}" Margin="0,0,0,10" Padding="10,5" Background="#4CAF50" Foreground="White"/>
            
            <Button x:Name="StopButton" Content="録画停止" Click="StopButton_Click" IsEnabled="False" Padding="10,5" Background="#AAAAAA" Foreground="White"/>
            
            <TextBox x:Name="LogBox" Height="80" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Margin="0,10,0,0"/>
            <TextBlock Text="このウィンドウを閉じるとトレイに最小化されます。" FontStyle="Italic" Foreground="Gray" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
